Firmware images are published here automatically when a version tag is pushed
in the private kestrel-one repository. `latest/` always holds the most recent
release, so the installer page can point at a URL that never changes.
